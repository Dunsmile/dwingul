# 뒹굴에 기여하기

오류 수정, 접근성 개선, 테스트 보강과 작은 콘텐츠 개선을 환영합니다. 큰 기능이나 규칙 변경은 먼저 [문의 이메일](mailto:poilkjmnb122@gmail.com)에서 범위와 기존 기록 호환성을 이야기해주세요.

## 시작하기

Node.js 24 이상이 필요합니다.

```sh
npm ci
npm test
npm run build
npm run check
```

변경 전 네 명령이 통과하는지 확인하고, 변경 후 같은 검증을 다시 실행합니다. 로컬 앱은 `npm start`로 열 수 있습니다. 프로필이나 기록을 만드는 수동 검증에는 실제 로컬 DB 대신 다음과 같이 메모리 DB를 사용합니다.

```sh
DW_DB=:memory: PORT=4174 npm start
```

이 서버는 종료할 때 데이터를 버립니다. 파일 보존을 확인해야 하는 테스트는 운영 데이터가 없는 임시 경로를 만들고 종료 후 지웁니다.

## 변경 원칙

- 한 Pull Request에는 한 가지 목적을 담고, 사용자에게 달라지는 동작과 검증 방법을 설명합니다.
- 게임 규칙, 점수 단위, 문제 묶음이나 결과 구조를 바꾸면 버전 호환성과 기존 공유 링크·기록을 함께 검토합니다.
- 성향 결과를 공인 MBTI나 검증된 심리검사로 표현하지 않습니다. IQ 추리를 표준화 검사로, 운세를 확정적 판단으로 소개하지 않습니다.
- 브라우저와 서버가 함께 쓰는 계약을 바꾸면 로컬 SQLite와 Cloudflare Durable Object SQLite 경로를 모두 검증합니다.
- 생성 파일을 직접 고치지 않습니다. 현재 일러스트 PNG는 `scripts/build-illustrated-art.mjs`와 `design/atlas-sources-v11.json`, 이전 SVG는 `scripts/generate-pixel-assets.mjs`, 정적 공개 페이지와 `dist/`는 `scripts/build-site.mjs`에서 수정합니다.
- 새 의존성은 꼭 필요한 경우에만 추가하고, `package-lock.json`을 함께 갱신합니다.
- 사용자 문구와 오류 메시지는 쉬운 한국어로 씁니다. 모바일 320px 폭에서도 조작과 읽기가 가능한지 확인합니다.

## 테스트 선택

모든 변경은 `npm test`, `npm run build`, `npm run check`를 통과해야 합니다. 여기에 변경 범위에 맞는 검증을 더합니다.

- 규칙·채점·저장 변경: 해당 단위 테스트와 API 통합 테스트
- 화면·입력 변경: 관련 Playwright 시나리오와 모바일·데스크톱 수동 확인
- 카탈로그·안내 변경: 공개 빌드 테스트와 생성된 콘텐츠 페이지 확인
- 프로필·공유 변경: 서로 다른 두 세션, 권한 경계, 삭제·복구 흐름 확인

브라우저 테스트가 필요하면 `npx playwright install chromium`으로 Chromium을 준비합니다. 화면 캡처와 보고서는 검토용 생성물이며 요청받지 않은 파일은 커밋하지 않습니다.

## 데이터와 비밀정보

다음 항목은 Git, 이슈, Pull Request, 테스트 fixture와 화면 캡처에 넣지 않습니다.

- `data/*.sqlite`, 운영 DB 내보내기와 실제 사용자 기록
- `.env*`, `.dev.vars*`, `.wrangler/`와 Cloudflare API 토큰
- 세션 쿠키, PIN, 복구 코드, 생년월일·태어난 시간과 공유 토큰
- 광고·분석 계정의 비밀키 또는 관리자용 값

예시가 필요하면 가짜 값임을 분명히 하고 개인을 식별할 수 없는 고정 fixture를 사용합니다. 커밋 전에 `git diff`와 `git status`를 확인해 생성물이나 비밀정보가 섞이지 않았는지 살펴봅니다. 실수로 비밀값을 커밋했다면 파일만 지우지 말고 해당 값을 즉시 폐기·교체한 뒤 관리자에게 알립니다.

## 이슈와 Pull Request

오류 제보에는 재현 순서, 기대한 동작, 실제 동작, 기기·브라우저와 관련 콘텐츠 이름을 적습니다. 공개 이슈에는 개인정보를 넣지 않습니다.

Pull Request 설명에는 다음을 포함합니다.

- 어떤 문제를 어떤 동작으로 바꿨는지
- 게임 규칙·저장 형식·기존 기록에 미치는 영향
- 실행한 검증 명령과 결과
- 화면이 바뀌었다면 필요한 최소 비교 자료

`.github/templates/ci.yml`은 Node.js 24에서 의존성을 새로 설치한 뒤 테스트, 공개 빌드와 문법 검사를 수행하는 CI 템플릿입니다. 현재 GitHub 연결의 workflow 권한이 없어 자동 실행 등록은 하지 않았습니다. 저장소 관리자가 템플릿을 `.github/workflows/ci.yml`로 옮기면 활성화됩니다. 템플릿에는 배포 토큰이 없으며 자동 배포하지 않습니다. 실제 배포 절차는 [docs/deployment.md](docs/deployment.md)를 따릅니다.

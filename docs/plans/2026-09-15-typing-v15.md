# Typing Master v15

User-authorized implementation and production release.

- Replace native HP/MP meters with one accessible pixel gauge, identical geometry in Chromium and WebKit.
- Present the typing hub as a forest title screen. Start, adventure settings, shop, equipment, characters and profile panels remain inside the same bounded game frame. Preserve mode/checkpoint when navigating; existing purchase, reveal, equip, enhancement and profile flows remain functional.
- Add a long sentence option: 250 unique Korean sentences, 20–30 characters including spaces. Traditional proverb adaptations and original wisdom prose are identified in source notes. Short mode retains its existing rules and record key; long mode has its own record key and doubles final attack damage, including equipment/combo. Shared inventory and unlocked chapters persist across modes.
- Server derives sentence pool and multiplier from saved run settings; submitted client rewards cannot override them. Friend rooms/shares retain the selected sentence mode and enforce baseline equipment/stage 1.
- Verify deterministic decks, exact ×2 attack, legacy short runs, server replay/rewards/records/shares, IME/pause, fixed help and responsive title/submenus/gameplay. Run full suite/build sequentially and official game client; visually inspect desktop/mobile screenshots. Test only isolated in-memory DB.
- Release via GitHub PR and Cloudflare; inspect production assets read-only.

Ownership: root handles integration, menus, meters, mode/replay logic and final QA/release. Single-hop content agent owns only long phrase module, provenance notes and content tests.
